{{#paper-item action="showFilter"}}Filter{{/paper-item}}
{{#if showFilter}}
  {{#paper-item}}
    <div layout>
      <div flex="20" layout layout-align="center center">
        <span>By Year</span>
      </div>
      <div flex="10" layout layout-align="center center">
        {{input type="number" value=yearFilter}}
      </div>
    </div>
  {{/paper-item}}
{{/if}}
