import { radiusByHeight } from 'meteorite-landings/helpers/radius-by-height';
import { module, test } from 'qunit';

module('Unit | Helper | radius by height');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = radiusByHeight([42]);
  assert.ok(result);
});
