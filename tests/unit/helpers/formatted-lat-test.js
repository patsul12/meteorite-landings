import { formattedLat } from 'meteorite-landings/helpers/formatted-lat';
import { module, test } from 'qunit';

module('Unit | Helper | formatted lat');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formattedLat([42]);
  assert.ok(result);
});
