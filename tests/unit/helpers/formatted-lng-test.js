import { formattedLng } from 'meteorite-landings/helpers/formatted-lng';
import { module, test } from 'qunit';

module('Unit | Helper | formatted lng');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formattedLng([42]);
  assert.ok(result);
});
